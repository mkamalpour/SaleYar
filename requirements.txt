# ============================================
# REQUIREMENTS FOR SALEYAR PROJECT
# ============================================
# Installation:
#   For normal use: pip install -r requirements.txt
#   For Conda users: See notes below for Conda packages
# ============================================

# ---------- CONDA PACKAGES (recommended via conda) ----------
# These packages work better with conda due to binary dependencies:
# pandas, numpy, scikit-learn, lightgbm, openpyxl, plotly, scipy
#
# To install them with conda:
#   conda install -c conda-forge pandas=2.1.4 numpy=1.26.3 scikit-learn=1.4.0
#   conda install -c conda-forge lightgbm=4.1.0 openpyxl=3.1.2 plotly=5.18.0 scipy=1.12.0

pandas==2.1.4
numpy==1.26.3
scikit-learn==1.4.0
lightgbm==4.1.0
openpyxl==3.1.2
plotly==5.18.0
scipy==1.12.0

# ---------- PIP PACKAGES (always use pip) ----------
# These are pure Python packages, fine to install with pip

statsforecast==1.7.0      # Time series forecasting
mlxtend==0.23.1           # Association rules (basket analysis)
ortools==9.9.3966         # Optimization engine
fastapi==0.109.0          # API framework
uvicorn==0.27.0           # ASGI server
gradio==6.14.0            # UI interface
jdatetime==5.0.0          # Persian date conversion
persiantools==3.4.0       # Persian tools
rapidfuzz==3.6.0          # Fast string matching
python-dotenv==1.0.0      # Environment variables
joblib==1.3.2             # Model persistence
rich==13.7.0              # Beautiful console output
threadpoolctl==3.1.0
seaborn==0.13.2
# ---------- NOTES ----------
# pytest is not included (only for development)
# uvicorn[standard] changed to uvicorn (avoid dependency issues in Docker)